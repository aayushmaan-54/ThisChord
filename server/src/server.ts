// import app from './app';
// import config from './config/config';

// app.listen(config.port, () => {
//   console.log(`Server running on port ${config.port}`);
// });


function add(a: number, b: number) {
  return a + b;
}

console.log("RES: ", add(3, 5));